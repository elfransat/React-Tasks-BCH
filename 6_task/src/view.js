import React, { Component } from "react";

class View extends Component {
  render() {
    return (
      <div>
        <h1 id="firstName">{this.props.firstName}</h1>
        <h1 id="firstName">{this.props.firstName}</h1>
      </div>
    );
  }
}

export default View;
